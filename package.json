{
  "name": "geom-normals",
  "version": "1.0.1",
  "description": "Compute normals for the geometry based on faces/cells information",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pex-math": "^3.0.0-0"
  }
}
